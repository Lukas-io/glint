name: flutter_network_mcp
description: MCP server exposing Flutter DevTools Network tab data to LLM agents via the Dart VM service.
version: 0.8.9
publish_to: none
repository: https://github.com/Lukas-io/flutter_network_mcp
homepage: https://github.com/Lukas-io/flutter_network_mcp
topics:
  - mcp
  - flutter
  - devtools
  - debugging
  - http

environment:
  sdk: ^3.5.0

executables:
  flutter_network_mcp:

dependencies:
  dart_mcp: ^0.5.1
  dtd: ^4.0.0
  vm_service: ^15.0.0
  args: ^2.5.0
  crypto: ^3.0.0
  sqlite3: ^3.0.0
  path: ^1.9.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
