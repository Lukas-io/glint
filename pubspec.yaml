name: glint_workspace
description: The glint repository root; each package under packages/ is released on its own.
publish_to: none

environment:
  sdk: ^3.6.0

workspace:
  - packages/glint_network
  - packages/glint_mcp

dependencies:
  args: ^2.5.0

dev_dependencies:
  test: ^1.24.0
